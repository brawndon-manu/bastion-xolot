/**
 * Registers or updates a device record
 * Not complete implementation
 */
export async function upsertDevice(device: any) {
    return {
        id: "temp-device-id",
        ...device
    };
}

/**
 * Returns known devices.
 */
export async function getDevices() {
    return [];
}